# simple-iam-admin-web 1.1.0 Changelog

## 发布信息

- 版本：`1.1.0`
- 类型：Feature / 向后兼容能力扩展
- 基线版本：`1.0.0`

## 主要变更

- 可信应用的 Portal 集成改用递归菜单树编辑器：支持目录与页面节点、受控图标、节点移动排序、四层结构校验和完整快照保存。
- PAGE 可绑定已申报的页面权限，并可选择标准布局或沉浸展示；为应用配置默认页面和静态子路径，平台管理员可设置无深链登录后的 Portal 首页。
- 可信应用详情抽屉加宽为双栏工作区，菜单树与节点编辑并排呈现；操作结果统一以右上角可关闭通知呈现，错误与成功均自动消失。
- 普通用户只加载当前权限允许访问的角色、组织和可信应用目录；仅拥有 `iam:user:page` 与 `iam:user:api` 时，用户列表和基础资料仍可用，不再因无权的附属目录接口导致整页失败。
- 管理端兼容 IAM Server 1.1 的菜单树、默认入口和 Portal 登录首页契约；旧扁平 `menus` 响应会在编辑时投影为根级 PAGE 节点。

## 兼容性

| 依赖 | 兼容范围 |
| --- | --- |
| IAM Server | `1.1.x` |
| IAM Contract | `1.1.x` |
| Unified Application Portal Web | 支持 `menuTree`、`defaultEntry` 与页面展示模式的版本 |
| IAM Theme Contract | `1.0.1` |

## 验证范围

- 单元测试覆盖菜单树旧数据投影、整树提交、图标与沉浸展示、默认入口、全局登录首页、Portal 版本冲突提示和最小用户管理权限。
- 浏览器测试覆盖生产构建中的可信应用菜单树编辑器、普通权限导航与最小用户权限下不请求无权目录接口。
- 本地真实入口走查覆盖平台管理员、最小用户管理权限和无应用准入用户；发布前需按本仓 `LOCAL_TEST_COMMANDS.md` 复验部署拓扑。
