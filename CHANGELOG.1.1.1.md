# simple-iam-admin-web 1.1.1 Changelog

## 发布信息

- 版本：`1.1.1`
- 类型：Feature / 向后兼容能力扩展
- 基线版本：`1.1.0`

## 主要变更

- 新增 Portal 可信应用根节点顺序管理：管理员可以在全量排序抽屉中定位应用、上移或下移，并提交带版本号的完整快照。
- 保留停用应用的排序位置，显示门户可见状态；搜索只用于定位，不会把局部结果提交为排序集合。
- 保存成功后，在门户壳提供受控刷新回调时立即更新导航；独立打开管理端时提示刷新门户后生效。
- 处理排序快照并发冲突：关闭编辑态、重新读取最新版本并提示管理员重新调整，不覆盖其他管理员的修改。

## 兼容性

| 依赖 | 兼容范围 |
| --- | --- |
| IAM Server | `1.1.x` |
| IAM Contract | `1.1.x` |
| Unified Application Portal Web | `1.1.1` 或兼容 `refreshPortalNavigation` 的版本 |
| IAM Theme Contract | `1.0.1` |

## 验证范围

- `vue-tsc`、ESLint、Vitest 覆盖率和生产构建通过。
- 单元测试覆盖排序 API 的版本化完整快照、成功保存后的门户刷新和 409 冲突提示。
- Playwright 生产构建测试通过；真实凭据走查按本地测试命令单独执行，不混入无凭据的默认检查。
